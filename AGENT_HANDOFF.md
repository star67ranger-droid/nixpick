# Handoff Cursor ↔ Antigravity

**Dernière mise à jour :** Cursor, 2026-09-23 (v0.3.5).

## Fait ensemble

- v0.3.4 : `fix-git`, preflight flake Git, Rofi « Corriger Git ».
- v0.3.5 : correctifs audit (CI git, packaging `share/nixpick`, fix-git ciblé flake, Dependabot, job `nix flake check`).

## Prochaine étape suggérée (une seule)

- Validation terrain Pikeo : `SUPER+N`, `nixpick doctor`, `nix flake lock --update-input nixpick` dans `/etc/nixos`.

## Pikeo seulement si

- `sudo` rebuild, `git commit` / `push`, arbitrage scope.
